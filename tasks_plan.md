# خطة العمل والتعديلات المطلوبة

تم فهم كافة المتطلبات وتقسيمها إلى 7 مهام رئيسية سيتم تنفيذها تباعاً:

## 1. ربط شاشة الاستقبال وأوامر العمل
- إضافة قائمة بأوامر العمل الحالية داخل شاشة الاستقبال (أو ربطها بشكل واضح).
- توفير زر "إضافة أمر عمل جديد" يسهل التنقل بين القائمة وإنشاء أمر جديد.

## 2. إصلاح مسار الرجوع في ساحة الورشة
- عند النقر على "رجوع" من داخل تفاصيل الصيانة في ساحة الورشة، سيتم إعادة توجيه المستخدم إلى شاشة `/work-orders` (أو `/status`) بدلاً من شاشة `/services` المخفية.

## 3. رقم الخانة (Bay Number)
- عند وصول المستخدم لخطوة "المراجعة والحفظ" في الاستقبال، سيظهر حقل لإدخال "رقم الخانة" (مثل رقم الموقف).
- الحقل سيكون إدخال نصي حر (Text Input) لتحديد مكان وقوف السيارة.
- سيتم عرض هذا الرقم لاحقاً في كافة شاشات ساحة الورشة.

## 4. تحسينات ساحة الورشة (العداد والواجهة)
- عرض "رقم الخانة" و "اسم الفني" بشكل واضح على بطاقة السيارة.
- تحويل وقت الصيانة إلى **عداد تنازلي حي (Live Countdown)** بدلاً من التصاعدي.
- العداد سيكون كبيراً، يبدأ باللون الأخضر ويتحول للأحمر عند انتهاء الوقت.
- تكبير حجم النصوص في ساحة الورشة لتكون مقروءة بسهولة للفنيين.

## 5. محول الفروع (Global Branch Selector)
- إضافة قائمة منسدلة (Dropdown) في الشريط الجانبي (Sidebar) تحت اسم "هندسة السيارات" مخصصة للـ Admin و Owner.
- عند اختيار فرع معين، سيتم تحديث كافة بيانات النظام (العملاء، الاستقبال، الساحة) لعرض بيانات هذا الفرع فقط بشكل مستقل تماماً.

## 6. تعديلات لوحة التحكم (Dashboard) والاستقبال
- **لوحة التحكم:** تحويلها إلى "لوحة إشعارات" تعرض: عدد السيارات اليوم، المكتملة، قيد العمل، وقيد الانتظار.
- إزالة "لوحة المتابعة" القديمة من الداشبورد لتوفير المساحة.
- توفير إشعارات حية ومباشرة للتحديثات والأوامر التي تحتاج طباعة.
- **الاستقبال:** عند اختيار "دفتر جديد"، سيتم تثبيت عدد التبديلات على "1" افتراضياً لتوفير وقت الكتابة.

## 7. تحديث قائمة الخدمات (Reception) والطباعة (PDF)
- إدراج **فلتر الكير** أسفل "هايدروليك الكير"، يحتوي على (نوع الفلتر، رقم الفلتر، السعر).
- تعديل **الماسحات (Wipers)** لتحتوي على: 
  - النوع: (VH, VP, VS).
  - الحجم: (14, 16, 18, 20, 22, 24, 26, 28 Inch).
- تعديل **المضافات والمحسنات**: تحويلها إلى قائمة ديناميكية تتيح إضافة أكثر من منتج (زر + لإضافة منتج جديد).
- **أحداث الصيانة**: نقل زر "إضافة حدث صيانة" ليكون في نفس الصف لسهولة الإضافة السريعة وتجنب التمرير لأعلى.
- **طباعة الـ PDF**: توفير خيارين للطباعة:
  1. **مختصر**: يطبع فقط الخدمات التي حالتها "يحتاج تغيير" أو الخدمات الإضافية المضافة.
  2. **كامل**: يطبع كافة الخدمات كما هو الوضع الحالي.
  - تقليل حجم الخطوط والجداول لضمان طباعة الفاتورة في **صفحة واحدة فقط (1 Page)**.
